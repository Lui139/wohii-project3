info:
  name: Search by keyword
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}?keyword=Venezuela"
  params:
    - name: keyword
      value: Venezuela
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
